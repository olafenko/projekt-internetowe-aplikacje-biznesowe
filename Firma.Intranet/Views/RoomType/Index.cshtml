@model IEnumerable<Firma.Data.Data.Hotel.RoomType>

@{
    ViewData["Title"] = "Typy pokoi";
}

<div class="view-container">
    <div class="header-row">
        <div class="title-group">
            <i class="bi bi-tags-fill"></i>
            <h1>Typy pokoi</h1>
        </div>
        <a class="createBtn" asp-action="Create">
            <i class="bi bi-plus-lg"></i> DODAJ TYP POKOJU
        </a>
    </div>

    <div class="filter-bar">
        <div class="filter-content">
            <i class="bi bi-funnel"></i>
            <span>SORTOWANIE I FILTROWANIE</span>
        </div>
    </div>

    <div class="table-card">
        <table class="table main-table">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                    <th><i class="bi bi-cash-stack me-2"></i>@Html.DisplayNameFor(model => model.BasePrice)</th>
                    <th><i class="bi bi-people me-2"></i>@Html.DisplayNameFor(model => model.MaxGuests)</th>
                    <th>@Html.DisplayNameFor(model => model.BedCount)</th>
                    <th><i class="bi bi-justify-left me-2"></i>@Html.DisplayNameFor(model => model.Description)</th>
                    <th><i class="bi bi-activity me-2"></i>Status</th>
                    <th class="text-center px-4">Akcje</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="fw-bold">@Html.DisplayFor(modelItem => item.Name)</td>
                        <td class="price-cell">@item.BasePrice.ToString("N2") ZŁ</td>
                        <td>@item.MaxGuests os.</td>
                        <td>@item.BedCount</td>
                        <td class="text-truncate">@item.Description</td>
                        <td>
                            @if (item.IsActive)
                            {
                                <span class="status-badge active">Aktywny</span>
                            }
                            else
                            {
                                <span class="status-badge inactive">Nieaktywny</span>
                            }
                        </td>
                        <td class="text-end px-4">
                            <div class="action-buttons">
                                <a asp-action="Edit" asp-route-id="@item.Id" title="Edytuj" class="edit-btn">
                                    <i class="bi bi-pencil-square"></i>
                                </a>
                                <a asp-action="Details" asp-route-id="@item.Id" title="Szczegóły" class="details-btn">
                                    <i class="bi bi-search"></i>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.Id" title="Usuń" class="delete-btn">
                                    <i class="bi bi-trash"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>