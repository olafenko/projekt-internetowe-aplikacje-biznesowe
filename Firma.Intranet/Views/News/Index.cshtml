@model IEnumerable<Firma.Data.Data.CMS.News>

@{
    ViewData["Title"] = "Aktualności";
}

<div class="view-container">
    <div class="header-row">
        <div class="title-group">
            <i class="bi bi-newspaper"></i>
            <h1>Aktualności</h1>
        </div>
        <a class="createBtn" asp-action="Create">
            <i class="bi bi-plus-lg"></i> DODAJ AKTUALNOŚĆ
        </a>
    </div>

    <div class="filter-bar">
        <div class="filter-content">
            <i class="bi bi-funnel"></i>
            <span>SORTOWANIE I FILTROWANIE</span>
        </div>
    </div>

    <div class="table-card">
        <table class="table main-table">
            <thead>
                <tr>
                    <th><i class="bi bi-fonts me-2"></i>@Html.DisplayNameFor(model => model.Title)</th>
                    <th><i class="bi bi-link-45deg me-2"></i>@Html.DisplayNameFor(model => model.LinkTitle)</th>
                    <th><i class="bi bi-file-text me-2"></i>@Html.DisplayNameFor(model => model.Content)</th>
                    <th><i class="bi bi-card-heading me-2"></i>@Html.DisplayNameFor(model => model.ContentSummary)</th>
                    <th><i class="bi bi-image me-2"></i>@Html.DisplayNameFor(model => model.ImageUrl)</th>
                    <th><i class="bi bi-calendar-event me-2"></i>@Html.DisplayNameFor(model => model.Date)</th>
                    <th><i class="bi bi-activity me-2"></i>Status</th>
                    <th class="text-center px-4">Akcje</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="fw-bold">@Html.DisplayFor(modelItem => item.Title)</td>
                        <td>@Html.DisplayFor(modelItem => item.LinkTitle)</td>
                        <td style="max-width: 150px;">@Html.DisplayFor(modelItem => item.Content)</td>
                        <td style="max-width: 150px;">@Html.DisplayFor(modelItem => item.ContentSummary)</td>
                        <td style="max-width: 100px;">@Html.DisplayFor(modelItem => item.ImageUrl)</td>
                        <td>@Html.DisplayFor(modelItem => item.Date)</td>
                        <td>
                            @if (item.IsActive)
                            {
                                <span class="status-badge active">Aktywna</span>
                            }
                            else
                            {
                                <span class="status-badge inactive">Nieaktywna</span>
                            }
                        </td>
                        <td class="text-end px-4">
                            <div class="action-buttons">
                                <a asp-action="Edit" asp-route-id="@item.Id" title="Edytuj" class="edit-btn">
                                    <i class="bi bi-pencil-square"></i>
                                </a>
                                <a asp-action="Details" asp-route-id="@item.Id" title="Szczegóły" class="details-btn">
                                    <i class="bi bi-info-circle"></i>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.Id" title="Usuń" class="delete-btn">
                                    <i class="bi bi-trash"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>