@model Firma.Data.Data.Hotel.Guest

@{
    ViewData["Title"] = "Usuń gościa";
}

<div class="view-container">
    <div class="row justify-content-center">

        <div class="col-8">
            
            <div class="header-row">
                <div class="title-group">
                    <i class="bi bi-person-dash"></i>
                    <h1>Usuń gościa</h1>
                </div>
                <a asp-action="Index" class="btn btn-outline-secondary d-flex align-items-center gap-2 backBtn">
                    <i class="bi bi-arrow-left"></i> POWRÓT
                </a>
            </div>

            <div class="form-card">
                
                <h4 class="text-danger mb-4">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i>Czy na pewno chcesz usunąć tego gościa?
                </h4>

                <h5 class="section-title"><i class="bi bi-person me-2"></i>Dane osobowe</h5>
                
                <div class="row">
                    <div class="col-md-6 form-group mb-3">
                        <label class="control-label fw-bold mb-1">@Html.DisplayNameFor(model => model.Name)</label>
                        <div class="form-control input-area d-flex align-items-center">
                            @Html.DisplayFor(model => model.Name)
                        </div>
                    </div>
                    <div class="col-md-6 form-group mb-3">
                        <label class="control-label fw-bold mb-1">@Html.DisplayNameFor(model => model.LastName)</label>
                        <div class="form-control input-area d-flex align-items-center">
                            @Html.DisplayFor(model => model.LastName)
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-6 form-group mb-3">
                        <label class="control-label fw-bold mb-1">@Html.DisplayNameFor(model => model.Email)</label>
                        <div class="form-control input-area d-flex align-items-center">
                            @Html.DisplayFor(model => model.Email)
                        </div>
                    </div>

                    <div class="col-6 form-group mb-4">
                        <label class="control-label fw-bold mb-1">@Html.DisplayNameFor(model => model.PhoneNumber)</label>
                        <div class="form-control input-area d-flex align-items-center">
                            @Html.DisplayFor(model => model.PhoneNumber)
                        </div>
                    </div>
                </div>
               

                <div class="row">
                    <div class="col-6 form-group mb-3">
                        <label class="control-label fw-bold mb-1">@Html.DisplayNameFor(model => model.IdentityCardNumber)</label>
                        <div class="form-control input-area d-flex align-items-center">
                            @Html.DisplayFor(model => model.IdentityCardNumber)
                        </div>
                    </div>

                    <div class="col-6 form-group mb-3">
                        <label class="control-label fw-bold mb-1">@Html.DisplayNameFor(model => model.Country)</label>
                        <div class="form-control input-area d-flex align-items-center">
                            @Html.DisplayFor(model => model.Country)
                        </div>
                    </div>
                </div>
                
                <div class="form-group mb-4 mt-3">
                    <label class="control-label fw-bold mb-1">@Html.DisplayNameFor(model => model.Notes)</label>
                    <textarea class="form-control input-area" rows="4" readonly>@Model.Notes</textarea>
                </div>

                <hr class="my-4 segment-divider" />

                <form asp-action="Delete">
                    <input type="hidden" asp-for="Id" />
                    
                    <div class="d-flex justify-content-end align-items-center gap-3">
                        <a asp-action="Index" class="btn btn-outline-secondary d-flex align-items-center gap-2 backBtn">
                            <i class="bi bi-arrow-left"></i> Anuluj
                        </a>
                        <button type="submit" class="btn btn-danger d-flex align-items-center gap-2 border-0 fw-bold px-4 py-2 rounded">
                            <i class="bi bi-trash me-1"></i> USUŃ
                        </button>
                    </div>
                </form>
                
            </div>

        </div>
    </div>
</div>